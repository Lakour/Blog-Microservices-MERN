# apiVersion: v1                  #specifies the set of object we want k8's to look at
# kind: Pod                       #type of object we want to create
# metadata:                       #config options for the object we are about to create
#   name: posts                   #give it a name when pod is created
# spec:                           #exact attributes we want to apply to the object    
#   containers:                   #we can create many containers in a single pod
#     - name: posts               #make a container with a name of posts
#       image: lakour/posts:0.0.1   #the exact image we want to use